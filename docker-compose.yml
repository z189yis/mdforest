services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mdforest
      POSTGRES_PASSWORD: mdforest
      POSTGRES_DB: mdforest
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mdforest"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
